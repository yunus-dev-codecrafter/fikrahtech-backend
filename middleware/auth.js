// Future authentication middleware
// This file will contain JWT verification and other auth-related middleware

// Example placeholder middleware:
// const authenticateToken = (req, res, next) => { ... };
// const verifyRole = (roles) => (req, res, next) => { ... };

module.exports = {
  // authenticateToken,
  // verifyRole
};